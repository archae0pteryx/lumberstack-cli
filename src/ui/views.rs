use tui::{backend::Backend, Terminal};

use crate::app_config::{self, AppConfig};
use anyhow::Result;

use super::app::App;

pub fn run_generate_view(app: &mut App) -> Result<()> {
    Ok(())
}

pub fn run_main_view(app: &mut App) -> Result<()> {
    Ok(())
}

pub fn run_tags_view(app: &mut App) -> Result<()> {
    Ok(())
}
